{
  "name": "esbuild",
  "private": true,
  "scripts": {
    "build": "node ./build.js",
    "watch": "node ./build.js --watch",
    "start": "node ./build.js --watch & http-server"
  },
  "dependencies": {
    "esbuild": "^0.14.5",
    "esbuild-plugin-elm": "^0.0.8"
  }
}
